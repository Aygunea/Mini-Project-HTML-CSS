// const swiperEl = document.querySelector('swiper-container');
// const swiper = swiperEl.swiper;

var swiper = new Swiper('.swiper-container', {
    // Gerekli ayarlar buraya eklenebilir
    pagination: {
      el: '.swiper-pagination',
    },
  });
